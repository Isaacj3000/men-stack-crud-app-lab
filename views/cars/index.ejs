<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head') %>
    <title><%= title %></title>
</head>
<body>
     <!-- Navigation Bar -->
    <nav class="teal">
        <div class="nav-wrapper">
            <a href="/" class="brand-logo center">🏎️ Dealership</a>
        </div>
    </nav>

    <div class="container">
        <h1 class="center-align">Car List</h1>
        <ul class="collection">
            <% for( let index = 0; index < cars.length; index++ ) { %>
                <li class="collection-item">
            <a href="/cars/<%= cars[index].id %>"><strong><%= cars[index].brand %></strong> (<%= cars[index].year %>)</a>
            </li>
            <% } %>
        </ul>
        <div class="center-align">
            <a href="/cars/new" class="btn wave-affect waves-ligth teal">Add a New Car</a>
        </div>
    </div>
</body>
</html>